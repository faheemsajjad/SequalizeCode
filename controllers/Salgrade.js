const router = require('express')();
const salObj  = require('../models/sequelize')['fs_salgrade'];

/*router.get('/',
    (req,res,next)=>{
        testUser
        .create({ name: 'John Doe'})
        .then(testUser => {
            console.log(testUser.get('name')); // John Doe (SENIOR ENGINEER)
        })
    }
);*/





module.exports = router;